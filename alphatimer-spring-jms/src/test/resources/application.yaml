spring:
  artemis:
    mode: embedded
    broker-url: tcp://localhost:57685
    embedded:
      enabled: true
      queues: converter.q

  jms:
    template:
      default-destination: converter.q

queue:
  boot: converter.q
